if status is-interactive
    # Commands to run in interactive sessions can go here
end

#Disable the Greetings
set -U fish_greeting

#Alias
alias la "ls -al"
alias .. "cd .."
alias cn "cargo new"
alias cr "cargo run"
alias yz yazi
